'use client';

import React from 'react';

// Basic layout for the awards section.
export default function AwardsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
